pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -al'
            }
        }
        stage('Test') { 
            steps {
                sh 'ls -al'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'ls -al'
            }
        }
    }
}
